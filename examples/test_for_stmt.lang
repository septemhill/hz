import "io"

fn main() i64 {
    for (1..10) |v| {
        io.println(v);
    }

    const c = [3]u8{'a', 'b', 'c'};
    for (c) |k, v| {
        io.println(k);
        io.println(v);
    }

    // Ignore indexs
    for (c) |_, v| {
        io.println(v);
    }

    // Ignore values
    for (c) |k, _| {
        io.println(k);
    }

    var f = Iter { i: 0 };

    for (f.next()) |e| {
        io.println(e);
    }

    var x = 0;
    for {
        if (x > 3) {
            break;
        }
        io.println("infinite loop");
        x += 1
    }

    outer: for (1..10) |v| {
        for (1..100) |b| {
            break outer;
        }
    }

    x = 0
    for (2 > 1) {
        if (x > 5) {
            break;
        }
        io.println("true expression");
        x += 1;
    }

    for (1..5) |v| {
        if (v > 3) {
            continue;
        }
        io.println(v);
    }

    return 0;
}

struct Iter {
    i: u8,

    pub fn next(self: *Self) ?u8 {
        if (self.i < 10) {
            const t = self.i;
            self.i += 1;
            return t;
        }
        return null;
    }
    
}
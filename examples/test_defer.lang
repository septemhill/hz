import "io"

fn cleanup() void {
    io.println("cleanup called");
}

fn test_defer() void {
    defer cleanup();
    io.println("test_defer body");
}

fn main() i64 {
    test_defer();
    io.println("main done");
    return 0;
}

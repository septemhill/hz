import (
  "io"
  m "math"
)

import "http"

fn main() i64 {
    io.println("Hello from grouped import!");

    http.head();

    const c = m.max(123, 343);
    io.println("c: {d}", c);
    
    return 0;
}

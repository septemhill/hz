fn main() i64 {
    // Access tuple 0 element.
    const t = (1, 2, 3);
    const x = t.0;

    // Destructure tuple, a = 1, b = 2, c = 3.
    const (a, b, c) = (1, 2, 3);

    // Return first element.
    return t.0;
}
